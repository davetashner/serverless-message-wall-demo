# Configuration for prod environment
# Used by CI to render Crossplane templates before publishing to ConfigHub
#
# These values are substituted into infra/base/*.yaml.template files
#
# IMPORTANT: Production environment has additional protections:
# - Delete gates on stateful resources (DynamoDB, S3)
# - Multi-party approval for HIGH risk changes
# - Policy enforcement with no exceptions
# See docs/tiered-authority-model.md for details.

# AWS Account ID (12 digits)
AWS_ACCOUNT_ID=205074708100

# AWS Region
AWS_REGION=us-east-1

# Resource prefix for naming (S3, Lambda, DynamoDB, etc.)
RESOURCE_PREFIX=messagewall

# Environment name
ENVIRONMENT=prod

# S3 bucket name (must be globally unique)
BUCKET_NAME=messagewall-demo-prod

# ConfigHub space name
CONFIGHUB_SPACE=messagewall-prod
